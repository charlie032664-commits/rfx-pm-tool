# AtlasRFQ Run Comparison — Baseline (v1.3)

Tool: `scripts/compare_model_outputs.py` (statistics only — no LLM, no pipeline).
Generated report (gitignored, not committed): `runs/atlasrfq_compare_report.md`.
This doc is a tracked summary; it contains **counts/distributions only**, no
customer requirement text.

## Runs compared

| side | run dir | provider / model |
|------|---------|------------------|
| left | `runs/2022_10_12_AtlasRFQ` | internal / `qwen3-coder-next` |
| right | `runs/_worker_test2/2022_10_12_AtlasRFQ` | internal / `qwen3-coder-next` |

Both are completed runs of the same case under the same provider/model.

## Results

| metric | left | right |
|--------|------|-------|
| requirements | 28 | 28 |
| total items / glossary / notes | 28 / 0 / 0 | 28 / 0 / 0 |
| compliance matrix rows | 28 | 28 |
| duplicate req_ids | 0 | 0 |
| missing fields | none | none |
| empty requirement text / category / owner | 0 / 0 / 0 | 0 / 0 / 0 |

### Category distribution (identical)
Wireless 7 · Power 6 · Network 4 · Mechanical 3 · CPU 2 · Storage 2 ·
Compliance 1 · General 1 · Memory 1 · Performance 1 (left == right).

### Must-level / Status distribution (identical)
- Must-level: INFO 28 (both)
- Status: NEED_REVIEW 28 (both)

### Output file availability
All present on both sides: `manifest.json`, `requirements.json`,
`requirements_enriched.json`, `requirements_clean.json`,
`requirements_review.xlsx`, `compliance_matrix.xlsx`.

## Conclusion

**PASS — zero statistical drift between the two AtlasRFQ runs.** Counts,
distributions, ids, and field completeness are identical.

## Note

This validates the comparison framework (`compare_model_outputs.py`) and
establishes a **baseline**: the same case + same internal model produces
equivalent statistics across separate runs. This baseline is the reference
against which any future **extraction-cache reuse** (see
`docs/cache_reuse_design.md`) must show zero drift before it can be enabled.
